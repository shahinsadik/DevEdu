/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['itder.com'], // Add 'itder.com' to allow images from this domain
      },
};

export default nextConfig;
